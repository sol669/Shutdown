<Window
    x:Class="ShutdownApp.ConfirmWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Shutdown">

    <Grid x:Name="RootGrid" Padding="28" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="8">
            <TextBlock x:Name="QuestionText" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
            <TextBlock x:Name="CountdownText" Opacity="0.68" />
        </StackPanel>

        <ProgressBar x:Name="CountdownBar" Grid.Row="1" Minimum="0" Maximum="100" Height="4" />

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="NoButton" Grid.Column="1" MinWidth="110" Click="NoButton_Click" />
            <Button x:Name="YesButton" Grid.Column="2" MinWidth="110" Style="{StaticResource AccentButtonStyle}" Click="YesButton_Click" />
        </Grid>
    </Grid>
</Window>
